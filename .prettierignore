node_modules
dist
.tsbuild
build
coverage
*.d.ts
bun.lock
tsconfig.tsbuildinfo
packages/create-zerotal
**/__test_migrations__

# Test fixtures are inputs to parsers — reformatting one changes what is being
# tested. ESLint ignores them for the same reason.
**/__fixtures__

# Auto-generated public-API surface snapshots (see scripts/api-surface.ts)
packages/*/api-surface.md

# Documentation vendored into the agent surface (see scripts/arch-docs.ts).
# A byte-for-byte copy of docs/ — formatting it here would make `arch:docs:check`
# fail against the source it was copied from.
packages/arch/docs

# Generated cast-audit baseline (see scripts/cast-audit.ts)
cast-baseline.json

# Bundler output — rewritten by every asset build, so formatting it would make this
# gate fail whenever someone runs the dev server.
**/public/assets/**
**/public/css/**
**/public/js/**

# Vendored icon bodies and the name union generated from them (see scripts/sync-icons.ts).
# Formatting either would fight the generator: `icons:check` compares generated output
# byte-for-byte, so a prettier pass over it fails a gate that is otherwise green.
packages/flow-ui/src/icons/lucide.json
packages/flow-ui/src/icons/names.generated.ts
packages/flow-ui/src/icons/brands.generated.ts

# The MCP tool contract snapshot (see scripts/mcp-surface.ts). Byte-compared by
# `mcp:surface:check`, so a prettier pass over it fails a gate that is otherwise green —
# the same reason the icon artifacts above are ignored.
packages/arch/mcp-surface.md
