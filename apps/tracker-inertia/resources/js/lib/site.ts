/**
 * Site-wide constants the UI reads.
 *
 * `APP_NAME` is the product's name, and it is the same string on the public page,
 * in the sidebar, on the sign-in screen and in the `<title>` — one name is the
 * cheapest way for a landing page and a dashboard to read as one product. It is
 * also `name` in config/app.ts and the `<title>` suffix in resources/js/app.tsx;
 * change all three together when you rename the app.
 */
export const APP_NAME = "Tracker";

/** Where the public page's "Docs" links point. */
export const DOCS_URL = "https://github.com/zerotaldev/zerotal";
