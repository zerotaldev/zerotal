// StartCommand is superseded by ServeCommand (commandName = 'serve', aliases: ['start', 's']).
// Re-exported for backwards-compatibility with code that imports this class directly.
export { ServeCommand as StartCommand } from "./ServeCommand.ts";
