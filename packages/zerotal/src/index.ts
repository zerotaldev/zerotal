/**
 * The `zerotal` meta package.
 *
 * `import { ... } from "zerotal"` is `@zerotal/core`; the rest of the stable
 * set is one subpath away (`zerotal/orm`, `zerotal/auth`, ...). Beta and
 * experimental packages are installed individually under their own names.
 */
export * from "@zerotal/core";
